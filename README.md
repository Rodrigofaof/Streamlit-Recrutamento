# Marketing Recruitment Pipeline

Pipeline modular com MySQL → Pandas → Fuzzy Match (RapidFuzz) → Excel + E-mail HTML.

## 1) Setup

```bash
python -m venv .venv
source .venv/bin/activate   

pip install -r requirements.txt
python main.py
